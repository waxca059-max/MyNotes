# My Notes Fullstack

一个基于 React 和 Node.js 的现代化全栈笔记应用，集成了 AI 辅助功能和 Markdown 实时预览。

## 🌟 核心特性

- **Markdown 编辑器**: 支持实时预览的高级 Markdown 编辑体验。
- **AI 智能助手**: 集成 Google Gemini API，提供智能写作建议和内容生成。
- **身份验证**: 基于 JWT 的安全用户认证系统（登录/注册）。
- **持久化存储**: 使用高性能的 SQLite (Better-SQLite3) 进行数据管理。
- **响应式界面**: 采用 Tailwind CSS 4 构建，适配多种屏幕尺寸，支持深层主题切换。
- **跨平台脚本**: 提供 Windows (.bat, .ps1) 和 Linux/macOS (.sh) 的一键启动/停止脚本。

## 🛠️ 技术栈

### 前端

- **框架**: React 19, TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS 4, Framer Motion (动画)
- **UI 组件**: Radix UI, Lucide React
- **功能库**: react-markdown, react-syntax-highlighter, sonner (通知)

### 后端

- **运行环境**: Node.js (Express 5)
- **数据库**: SQLite (Better-SQLite3)
- **AI 接口**: Google Generative AI (@google/generative-ai)
- **安全性**: JWT (jsonwebtoken), bcryptjs (密码加密)
- **文件处理**: Multer

## 🚀 快速开始

### 1. 克隆与安装

在项目根目录下，运行以下命令自动安装所有依赖（包括 client 和 server）：

```bash
npm run install:all
```

### 2. 配置环境

在 `server` 目录下创建 `.env` 文件，并配置以下环境变量：

```env
PORT=3002
JWT_SECRET=your_jwt_secret_key
GEMINI_API_KEY=your_gemini_api_key
```

### 3. 运行项目

你可以使用以下方式之一启动开发环境：

#### 命令行方式

```bash
# 同时启动前后端（建议分窗口运行）
npm run dev:client  # 前端: http://localhost:3001
npm run dev:server  # 后端: http://localhost:3002
```

#### 一键脚本方式 (推荐)

- **Windows**: 双击 `一键启动服务.bat` 或运行 `start.ps1`。
- **Linux/macOS**: 运行 `./start.sh`。

## 📁 项目结构

```text
my-notes-fullstack/
├── client/           # 前端 React 应用
│   ├── src/
│   │   ├── components/ # UI 组件
│   │   ├── hooks/      # 自定义 Hooks
│   │   └── lib/        # 工具函数
├── server/           # 后端 Express 应用
│   ├── data/         # 数据库文件
│   ├── uploads/      # 上传的文件
│   └── index.js      # 入口文件
├── scripts/          # 自动化脚本 (.bat, .ps1, .sh)
└── package.json      # 根目录管理脚本
```

## 📜 许可证

本项目采用 ISC 许可证。
